# Video Server

This module is responsible for:
- Connecting to RTSP streams or local cameras.
- Capturing frames continuously.
- Serving frames to clients (Testing Server) upon request via Sockets.

## Usage
```bash
python video_server.py <rtsp_url_or_camera_id>
```
